# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Local tooling at the root (never shipped; the site has no build step).
# play-src/ keeps its package.json and lockfile under version control.
node_modules/
/package.json
/package-lock.json
.claude/

# Wrangler / Cloudflare local state
.wrangler/
.dev.vars
.env
.env.*
